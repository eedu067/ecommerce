services:
  postgres:
    image: postgres:18.6-bookworm
    container_name: ecommerce-database
    env_file:
      - .env
    ports:
      - "5432:${POSTGRES_PORT}"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: